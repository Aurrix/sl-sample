import * as contextParserModule from './context/context-parser.js';
Object.assign(window, contextParserModule);
import * as stateComponentModule from './context/state-component.js';
Object.assign(window, stateComponentModule);
import * as socketModule from './socket/socket.js';
Object.assign(window, socketModule);

